//! `wallpaper-settings` — standalone libcosmic GUI for the dynamic wallpaper daemon
//! (spec 7 US1). See `README.md` for scope; `app.rs`/`pages/` hold the real
//! implementation (Phase 3).

fn main() {
    todo!("Phase 3 (T024) implements the real cosmic::Application entry point")
}
